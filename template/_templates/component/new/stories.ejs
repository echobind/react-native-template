---
to: src/components/<%= h.inflection.undasherize(name) %>/<%= h.inflection.undasherize(name) %>.stories.tsx
---
<% const formattedName = h.inflection.undasherize(name) -%>
import { storiesOf } from '@storybook/react-native';
import React from 'react';

import { <%= formattedName %> } from './component';

storiesOf('components/<%= comp %>', module).add('Default', () => (
  <<%= formattedName %> />
));
