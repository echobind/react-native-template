---
to: storybook/stories/<%= h.inflection.transform(name, ['undasherize', 'classify']) %>.stories.tsx
---
<% const formattedName = h.inflection.transform(name, ['undasherize', 'classify']) -%>
import { storiesOf } from '@storybook/react-native';
import React from 'react';

import { <%= formattedName %> } from '../../src/components/<%= formattedName %>';

storiesOf('components/<%= formattedName %>', module).add('Default', () => (
  <<%= formattedName %> />
));
